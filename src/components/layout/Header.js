import React from "react";

function Header() {
  // This component is not used in the new design.
  // The landing page has its own built-in header.
  // This file can be safely deleted.
  return null;
}

export default Header;
